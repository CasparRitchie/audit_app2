{
  "name": "audit-app",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "dropbox": "^10.34.0",
    "react-bootstrap": "^2.10.4"
  }
}
